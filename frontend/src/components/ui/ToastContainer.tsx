/**
 * ToastContainer
 * Renders queued toast notifications.
 * Placeholder — will be fleshed out in a follow-up.
 */
export function ToastContainer() {
  return <div id="toast-container" className="fixed top-4 right-4 z-50" />
}
